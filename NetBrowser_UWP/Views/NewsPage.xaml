<Page
    x:Class="NetBrowser_UWP.Views.NewsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NetBrowser_UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:NetBrowser_UWP.Models"
    xmlns:viewModels="using:NetBrowser_UWP.ViewModels"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    d:DataContext="{d:DesignInstance Type=viewModels:NewsPageViewModel,IsDesignTimeCreatable=True}"
    controls:BackdropMaterial.ApplyToRootOrPageBackground="True">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Text="Новости" FontSize="40" Grid.Row="0"
                   Margin="50,0,0,0"/>
        <ProgressRing Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Center" IsActive="{Binding IsProgressRingActive}"></ProgressRing>

        <Grid Grid.Row="1" Grid.Column="1" Padding="5" Background="{Binding Source={StaticResource ThemeManager}, Path=ThirdBrush}" 
              Margin="15" CornerRadius="8">
            <ListView ItemsSource="{Binding News, UpdateSourceTrigger=PropertyChanged}" CanReorderItems="True" 
                      ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollBarVisibility="Visible" 
                      ScrollViewer.IsVerticalRailEnabled="True">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:ContentModel">
                        <Grid Margin="0,12,0,12" AutomationProperties.Name="{x:Bind Title}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MinWidth="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="{x:Bind ImageUrl}" MaxHeight="100" MaxWidth="200" Stretch="UniformToFill"/>
                            <StackPanel Margin="12,0,0,0" Grid.Column="1">
                                <TextBlock Text="{x:Bind Title}" FontSize="14" FontWeight="SemiBold" Style="{ThemeResource BaseTextBlockStyle}"
                                           HorizontalAlignment="Left" Margin="0,0,0,6"/>
                                <TextBlock Text="{x:Bind Description}" Style="{ThemeResource BodyTextBlockStyle}"
                                           TextTrimming="CharacterEllipsis" MaxLines="2" HorizontalTextAlignment="Left"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{x:Bind PubDate}" Style="{ThemeResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>

        </Grid>
        

    </Grid>

</Page>
