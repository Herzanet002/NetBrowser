<Page
    x:Class="NetBrowser_UWP.Views.News.NewsSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NetBrowser_UWP.Views.News"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:news="using:NetBrowser_UWP.ViewModels.News"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=news:NewsSettingsViewModel,IsDesignTimeCreatable=True}"
    Background="{Binding Source={StaticResource ThemeManager}, Path=ThirdBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource PageTitleStyle}" Text="Параметры" Margin="50,20" />
        <Grid Row="1">
            <StackPanel Spacing="20" Margin="60,0">
                <TextBlock Text="Рекомендуемые новости:" Style="{StaticResource PageSubTitleStyle}" />
                <Button Content="Очистить рекомендуемые категории" Command="{Binding ClearRecommendCategoriesCommand}"></Button>
            </StackPanel>

        </Grid>
    </Grid>
</Page>
